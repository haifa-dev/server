/**
 * TIP: Use vscode 'Change Language Mode' command (Ctrl/Cmd + Shift + P)
 * To view this as a javascript file
 */

const { resolve } = require('path');

module.exports = {
  'config': resolve('config/database.js'),
  'models-path': resolve('db/models'),
  'seeders-path': resolve('db/seeders'),
  'migrations-path': resolve('db/migrations')
};
